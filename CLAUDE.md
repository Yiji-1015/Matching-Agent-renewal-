# CLAUDE.md — 이 저장소에서 일하는 방식

## 프로젝트
KCI 투고용 논문 「Loose Matching 기반 역할 분화 멀티에이전트 매칭 시스템」.
윤이지 석사학위논문(`docs/석사학위논문.pdf`)을 교신저자가 KCI 원고로 다시 쓰는 중이고,
저자(윤이지)가 그 원고를 받아 **인용 정확성을 검증·정정**한다.

- 교신저자 원고(정본): `docs/KCI_논문초안.docx`
- 독립 재작성 초안(대조용, 정본 아님): `docs/kci_draft_claude.md`
- 참고문헌 PDF: `참고문헌 모음/` — 확보 필요 목록은 `참고문헌 모음/_다운로드_목록.md`
- 인간평가 원자료: `docs/ratings_ver3.xlsx`
- 검수 진행 상황: `docs/검수_진행상황.md` ← **세션 시작 시 반드시 읽을 것**

## 검수 규칙 (반드시 지킬 것)

1. **모든 인용은 원문을 직접 열어 대조한다.** PDF는 `pdftotext`로, 없으면 웹에서 원문 확인.
   기억이나 추측으로 판단하지 않는다.
2. **서지정보는 정식 게재본 기준으로 확인한다.** arXiv 프리프린트면 학회·저널 게재 여부를
   검색해 게재본 서지로 바꾼다.
3. **원문에 없는 주장·예시·수치가 붙어 있는지 확인한다.** 인용문이 원문보다 강한 주장을
   하고 있으면 지적한다. 절 번호(§) 참조도 원문에서 실제 위치를 확인한다.
4. **석사논문의 참고문헌 목록은 신뢰하지 않는다.** 저자명 오기가 많다. 항상 원문으로 확인.
5. **교신저자의 문장은 최대한 살린다.** 문체를 새로 쓰지 말고, 사실이 틀린 부분만 고친다.

## 출력 형식

수정 결과는 **원문 그대로 채팅에 출력**하되, 고친 부분만 `**볼드**`로 표시한다.
파일을 말없이 덮어쓰지 않는다. 확정된 수정만 사용자의 지시로 반영한다.

## 통계 수치
`docs/검수_진행상황.md`의 "확정 수치" 절을 따른다. 특히 ICC 0.804와 `rating3` 시트 기반
수치는 **사용 금지**(사유는 해당 문서에 기재).
