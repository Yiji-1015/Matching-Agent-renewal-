# 실행표 — HWP 편집하면서 이것만 보면 됨

붙여넣을 긴 글(초록·결론·부록 등)은 **`붙여넣기_0906.md`** 에 있다.
왜 그렇게 고치는지 궁금할 때만 `HWP반영_지시서_0906.md`를 열면 된다. 평소엔 안 봐도 된다.

---

## 1단계. 찾아 바꾸기 16건

`Ctrl+F2`(찾아 바꾸기)에 아래를 그대로 복사해 넣는다.
**모두바꾸기 칸이 ✅면 "모두 바꾸기"를 눌러도 된다.** ⚠️는 하나씩 확인하면서 바꾼다.

| | 찾을 내용 | 바꿀 내용 | 모두바꾸기 |
|---|---|---|---|
| 1 | <code>&nbsp;(Ren et al., 2021)</code> ← **맨 앞 공백 포함** | `(Ren et al., 2021, p.333)` | ⚠️ 아래 주의 |
| 2 | `(Gale and Shapley, 1962)` | `(Gale & Shapley, 1962)` | ✅ |
| 3 | `것이다.예컨대` | `것이다. 예컨대` | ✅ |
| 4 | `세 과제를 수행해야 한다고 보았다(Steiner, 2019)` | `세 과제를 수행해야 한다고 보았다` | ✅ |
| 5 | `구현한다 본 시스템이` | `구현한다. 본 시스템이` | ✅ |
| 6 | `매칭의 첫 단계가 된다(Ren et al., 2021)` | `매칭의 첫 단계가 된다` | ✅ |
| 7 | `해석하는 관점이다(Steiner, 2019)` | `해석하는 관점이다` | ✅ |
| 8 | `세 가지 범주로 나뉜다(Gao et al., 2023)` | `세 가지 범주로 나뉜다` | ✅ |
| 9 | <code>&nbsp;(Suzgun and Kalai, 2024)</code> ← **맨 앞 공백 포함** | `(Suzgun & Kalai, 2024)` | ✅ |
| 10 | `(Wu et al., 2023)` | `(Wu et al., 2024)` | ✅ |
| 11 | `후보 선별. 평가` | `후보 선별, 평가` | ✅ |
| 12 | `<그림 2>과 같다` | `<그림 2>와 같다` | ✅ |
| 13 | `구매&#8211;판매` | `구매–판매` | ✅ |
| 14 | `Feinstein and Cicchetti, 1990` | `Feinstein & Cicchetti, 1990` | ✅ |
| 15 | `이러한 편중 상황을 위해 설계된` | `이러한 편중 상황에 대응하여 설계된` | ✅ |
| 16 | `또한  베이스라인과` ← **공백 두 칸** | `또한 베이스라인과` | ✅ |

> ⚠️ **1번만 조심.** `(Ren et al., 2021)`은 문서에 2번 나오는데 6번에서 하나를 지운다.
> **6번을 먼저 실행하고 1번을 나중에** 하면 남는 게 하나뿐이라 모두 바꾸기를 눌러도 안전하다.
> (순서를 바꾸고 싶으면 1번을 찾을 때 반드시 맨 앞 공백을 포함할 것)

**문장 2개는 통째로 바꾼다** (찾아 바꾸기 말고 직접 고침)

| | 어디 | 무엇 |
|---|---|---|
| 17 | 2.3절 — `GPT-4 기준으로 표준 프롬프팅 대비 17.1%…`로 **시작하는 짧은 문단** | 문단 전체를 지우고 → `붙여넣기_0906.md` **[17]** 을 넣은 뒤, **바로 윗 문단 끝에 이어 붙여** 한 문단으로 만든다 |
| 18 | 5.4절 — 첫 문단 **맨 끝 문장** `또한 응답에 사용되는 표본은 베이스 라인 대비 교정률 평가의 challenge set과 별개로 구성되었다.` | **그 문장만 삭제** (같은 문단 앞쪽에 같은 말이 이미 있음) |

---

## 2단계. 문장 넣기 5곳

방법은 전부 같다 — **앵커 문단을 찾아서, 그 문단 맨 끝에 커서를 놓고, 이어서 붙여넣는다.**
(새 문단을 만들지 말고 기존 문단 뒤에 그냥 붙인다. 20번만 예외)

| | 어느 절 | 앵커 = 이 단어로 시작하는 문단 | 넣을 글 |
|---|---|---|---|
| 19 | 1. 서론 | **「그러나」** 로 시작하는 문단 (2문단) | `붙여넣기_0906.md` **[19]** |
| 20 | 2.2절 | **「임베딩」** 으로 시작하는 문단 (첫 문단) | **[20]** |
| 21 | 2.2절 | **「쿼리」** 로 시작하는 문단 (IRCoT 나오는 문단) | **[21]** |
| 22 | 2.3절 | **「또한」** 으로 시작하는 문단 (ChatDev 나오는 문단) | **[22]** |
| 23 | 4.1절 | **「메시지는」** 으로 시작하는 문단 — ⚠️ **문단 끝이 아니다.** 맨 마지막 문장 `구체적인 기준의 내용은 다음과 같다.` **바로 앞**에 끼워 넣는다 | **[23]** |

**한 곳 더 — 문단 합치기**

| | |
|---|---|
| 24 | 1. 서론에서 **「Steiner(2019)는」** 문단이 `…정보를 바탕으로` 에서 끊기고, 다음 문단이 `선별(Screening),` 로 시작한다. **사이의 줄바꿈을 지워 한 문단으로 합친다.** (3번 항목을 여기서 같이 처리) |

**각주 1개**

| | |
|---|---|
| 25 | 5.2절 **「5.3절의」** 로 시작하는 문단에서 `…측정하였다.` 뒤에 각주를 달고, 면 하단에 `붙여넣기_0906.md` **[25]** 를 넣는다. 🔴 **저자 이름을 쓰지 말 것** — 블라인드 심사라 실명이 들어가면 안 된다 |

---

## 3단계. 통째로 교체 5곳

**기존 내용을 먼저 다 지우고** 붙여넣는다. 본문은 전부 `붙여넣기_0906.md`에 있다.

| | 어디 | 지울 것 | 넣을 것 |
|---|---|---|---|
| 26 | `< Abstract >` 아래 | 샘플 영문초록 + `Key Words:` 줄 | **[A-2]** |
| 27 | `< 국문초록 >` 아래 | 샘플 국문초록 + `주제어:` 줄 | **[A-1]** |
| 28 | `4.1.4. 역할 상보성` | 본문 1문단 (`요청자와 응답자의…`) | **[A-3]** |
| 29 | 6장 | ① `6. 시사점 및 연구의 한계` **장 전체 삭제**<br>② 제목 `6. 결론 (써야함)` → `6. 결론`<br>③ 그 아래 샘플 결론 전부 삭제 | **[A-4]** (5문단) |
| 30 | `부록 – 설문문항` | 샘플 표(TIO1~6 / ITU1~4) 표째로 삭제 | **[A-5]** |

---

## 4단계. 참고문헌

| | 할 일 |
|---|---|
| 31 | 템플릿 샘플 9건을 지우고, **`참고문헌_0904.hwp`의 목록(국내 4 + 국외 33)을 통째로 가져온다** |
| 32 | 국외에서 **`Palmer, M., Gildea, D., & Xue, N. (2010)`** 항목 삭제 |
| 33 | 찾아 바꾸기: `Systems 36 (pp. 20827–20905).` → `Systems 36 (NeurIPS 2023), New Orleans, LA, USA.` |
| 34 | 목록 맨 끝 **`[URL]` 머리표 삭제** (아래에 항목이 하나도 없음) |
| 35 | 🔴 **DOI 28건 전부 삭제** — `https://doi.org/…` 를 앞의 공백까지 포함해 지운다 |
| 36 | 🔴 **번호를 손으로 치지 말 것.** 참고문헌 문단 전체 선택 → 스타일에서 `참고문헌` 적용 → 번호가 자동으로 붙는다.<br>**확인법: 국외 첫 항목(Ayala)이 5번**이면 정상. 1번이면 잘못된 것 |
| 37 | Wang(2025)·Tsagkias(2020) 항목은 **이미 맞다. 손대지 말 것** |

---

## 5단계. 마지막

| | |
|---|---|
| 38 | 따옴표 통일 — `"바람막이”` `‘아우터'` 처럼 여닫이가 섞여 있다 |
| 39 | 부록 표에 상호·연락처·특정 지명 남았는지 훑기 (특히 **28번 상호명**, **14번 스터디 시간·장소**) |
| 40 | 분량 — 15페이지 넘으면 부록 `<표 A-2>`(약 2쪽)를 빼는 게 첫 카드 |

---

### 보류 중인 것 하나

페이지 병기를 1번 한 곳에만 붙일지, 2.2절(Wang 예문)·2.3절(17.1% 수치)까지 3곳에 붙일지는
아직 안 정했다. **지금은 1곳만 붙이는 상태**이고, 그대로 둬도 규정 위반은 아니다.
